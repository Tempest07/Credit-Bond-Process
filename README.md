# Bond Centre

信用债投资流程意见生成器。粘贴固定格式的项目简表后，页面会解析债券信息、匹配主体最新授信、应用投资比例与终批规则，并生成可编辑的流程意见。

## v4.2.0 · 后台发行结果识别队列（2026-09-04）

- “结果”改为按钮下方的紧凑录入浮层，不再遮挡整页，也不再因打开录入而展开整张项目详情。
- 可连续将多个项目加入后台队列，严格逐笔调用语义识别；完成后在右下角提示，点击核对并人工确认后才写入台账。
- 相对缴款日支持“明天缴款、周一缴款、下周一缴款”，全部以用户核对的原通知日期为锚点；模型漏提时只从有明确缴款语义的原文补齐。
- 发行结果录入浮层改为结果按钮的文档内锚定子元素；页面滚动时与按钮一起移动，不再固定在视口右上角。
- 后台识别状态改为按钮外沿光效：处理中显示双层外扩波纹，完成待核对后显示柔和呼吸光晕，不再与按钮点击涟漪冲突。
- 本次产品版本为 `4.2.0`，当前内部构建 `4.2.0.5`。

### 构建 4.2.0.5 · DM 同主体债券候选

新增项目读取 DM 时，如果本期简称没有真实返回、但云端主体库能够确认发行人，系统会继续读取该发行人的 DM 存续债，并按主体简称、发行年份、品种和期次给出最多 5 个可点击候选。候选只供人工选择；点击后才会按所选债券重新读取，不会自动把近似债券写入当前项目。

## v4.1.1 · 版本标记（2026-09-04）

- 页面展示、应用元数据与包版本统一为 `4.1.1`。
- 内部构建号继续使用 `4.0.0.5`；本次仅更新产品版本，不改变业务逻辑和数据格式。

## v4.0.0 · 发行结果语义识别（2026-09-03）

发行结果录入升级为“模型语义识别 → 原文证据校验 → 分品种预览 → 人工确认写入”。使用 Cloudflare Workers AI 托管的 OpenAI `gpt-oss-120b`，支持不固定格式的通知、多品种、取消发行与回拨信息；缺失信息不自动补齐，结果仍需校验与人工复核。

本次大版本标记对应内部构建 `4.0.0.1`，不改变既有项目数据格式、模型配置或业务规则。模型调用与验收边界详见 [发行结果语义识别说明](./SEMANTIC_ISSUANCE.md)。

### 构建 4.0.0.2 · 最终标位确认

项目筛选统一为“未投标 / 已投标 / 已投标结束 / 已出结果”。提交标位后仍可继续改标；点击“确认最终标位”后转入“已投标结束”，也可随时点击“继续改标”恢复为“已投标”，保留所有提交历史。旧的“已投标待结果”记录归入“已投标”，不会被自动认定为最终标位。具体状态与验收见 [最终标位工作流](./BID_FINALIZATION.md)。

### 构建 4.0.0.3 · 卡片标位提示

尚未完成缴款的项目卡片显示最近提交的投标利率与金额，支持多档、分品种及委外标位；最终标位和未提交的修改分别标注。缴款完成后隐藏，未投标或不投标终止项目不显示已提交标位。

### 构建 4.0.0.4 · 截标日期预览与保存一致

新增页预览和保存使用同一套截标日期规则。匹配到同名台账时，“智能”明确显示并保留已存日期，避免覆盖手工延期；选择“今天 / 下一工作日”后会实际更新已有日期并留下改期记录。单项目确认保存使用页面已显示的日期，不因跨越默认截标时刻或午夜而重新推算；批量项目独立计算，不借用单笔预览。

### 构建 4.0.0.5 · 协议转让日期识别

修复将 `2.9Y` 剩余期限误识别为2月9日、再推至次年的问题。数字月日须有交易日标注、交易所/交易等日期上下文或为单独日期；期限、收益率和报价片段不再作为日期候选。用印日仍按识别出的交易日推导；没有可靠日期时保留原有默认当天行为，并显示待复核提示，不标记为识别成功。

## 当前能力

- 解析债券简称、主承身份、分行、期限、规模、评级、询价区间、发行场所、牵头主承、估值和指导价
- 根据简称匹配主体与普通信用债 50206 授信
- 自动生成常见银行间债券全称：SCP、CP、MTN、PPN
- 计算建议投资比例与金额
- 自动判断处室、金处、周总或房地产债林总终批
- 一级投标利率固定留作待填写
- 支持浏览器本地资料库与 Cloudflare D1 同步
- 云端状态使用 revision 并发校验；多个标签页或设备同时保存时，过期写入会保留为冲突快照，不再静默覆盖当前数据
- 支持查看最近 50 个资料库快照的保存时间、设备来源和增删改摘要，并可导出或回溯为新版本
- 页面在前台 15 分钟无操作时提前 60 秒提示，等待云端保存确认后通过 Gateway 安全退出；保存失败时不会退出
- 支持 JSON 导入导出备份
- 支持在浏览器本地解析历史 Word 流程文档，按“越靠前越新”归并最新授信
- 普通信用债 50206 与 ABS 50217 分开维护：每个主体仅保留一张 50206；同一 ABS 增信方可维护多张 50217
- 主体授信库以主体为入口，可在同一主体下切换 50206 / 50217，并查看 50217 批单数量与明细
- 50217 支持单项目批与储架批；单项目批只匹配指定专项计划，储架批可被同一增信方的多个 ABS 项目关联
- 新增 ABS 项目时可就地新增 50217 并立即关联；增信方未入库时可同时补录主体资料，无需离开项目录入页
- 历史 Word 异常记录支持在导入前人工修正或排除
- 单笔生成页可直接录入或更新当前主体授信
- 支持一次粘贴多笔项目简表，批量生成、复制和录入新主体授信
- 支持交易所标准公司债全称生成；简表可注明“公开/非公开”或“公募/私募”
- 支持银行间及交易所双品种互拨项目，自动合并简称并分别列示期限、询价区间和建议比例
- 支持将流程意见保存为项目台账，通过看板区分未投标、仍可改标、最终标位已确认和已出结果，并保留中标与缴款明细筛选
- 支持按品种记录投标利率、投标量、中标利率、中标量，并自动生成结果摘要
- 支持记录综合定价扣税后营收、FTP，并自动计算扣除 FTP 后收益
- 支持专用邮箱自动接收缴款单，保留原始邮件与 PDF，识别多项目、续页和空白页
- 支持在项目品种内直接查看已对应缴款单，并按日期查看全部归档和人工复核候选
- 支持通过文件管理器弹窗按“缴款单 → 缴款日期 → 原始 PDF”直观浏览邮箱原件
- 支持“应收单据对账”，同时提示已缴款和未缴款中标品种的缺单情况
- 支持按原 PDF 页码人工修正多项目、续页和空白页分组，原始文件始终保留
- 自动匹配缴款单不会改变缴款状态；缴款仍须人工确认

## 规则

- 交易所债券简称通常不能可靠反推出公开或非公开发行，尾号也不能可靠反推出发行期次。发行方式和期次均以明确标注或人工确认为准。

1. 普通信用债建议投资比例默认使用 50206 批复比例；ABS 只使用所关联 50217 批单的比例，不回退到 50206。
2. 兴业银行牵头或联席主承时，比例最高为20%。
3. 隐含评级AA且期限超过授信投资期限时，比例最高为15%。
4. 隐含评级AA(2)且期限超过授信投资期限时，比例最高为10%。
5. 投资金额等于发行规模乘以建议比例。
6. 房地产债由林总终批。
7. 隐含评级AAA：投资金额超过8亿由金处终批，超过10亿由周总终批。
8. 其他隐含评级：投资金额超过3.2亿由金处终批，超过4亿由周总终批。
9. 同一主体的新授信覆盖旧授信；Word批量导入时，文档越靠前的记录越新。

## Cloudflare Pages

1. 创建 GitHub 仓库 `Tempest07/Credit-Bond-Process` 并推送本目录。
2. 创建 Cloudflare Pages 项目，连接该仓库。
3. Framework preset 选择 `None`，Build command 留空，Build output directory 填写 `.`。
4. 创建 D1 数据库 `credit-bond-process`。
5. 在 Pages 项目的 Settings / Bindings 中添加 D1 binding：
   - Variable name：`DB`
   - D1 database：`credit-bond-process`
6. 在 Pages 项目的 Settings / Variables and Secrets 中添加加密 Secret：
   - Variable name：`GATEWAY_AUTH_SECRET`
   - Value：与 `tempest07.com` Gateway Worker 中的 `GATEWAY_AUTH_SECRET` / `TEMPEST_AUTH_SECRET` 保持一致，用于校验 gateway 注入的短期签名身份
7. 为债券数据查询配置服务端 Secrets：
   - `INNO_APP_KEY` / `INNO_APP_SECRET`：DM 债券档案、发行、主体评级与评级机构
   - `WIND_API_KEY`：Wind 中债隐含评级；仅由 Pages Function 读取，不下发到浏览器
8. 重新部署 Pages。

缴款单系统还需要 R2、Queue、Email Routing 和独立 Email Worker，详见 [PAYMENT_RECEIPTS_DEPLOYMENT.md](./PAYMENT_RECEIPTS_DEPLOYMENT.md)。

Pages Function 会在首次通过 gateway 访问资料库时自动创建所需表、为现有状态补充 revision 和初始快照，并把旧 `app_state` 数据迁移到 `admin` 管理员账号名下；也可以手动执行 `schema.sql`。单份状态在写入前限制为 1,800,000 bytes，以避开 D1 单行容量边界；达到限制时应先导出备份并拆分数据模型。

## Gateway

在 `tempest07.com` Gateway Worker 的 `ROUTES` 数组中增加：

```js
{
  prefix: "/bond-centre",
  origin: "https://credit-bond-process.pages.dev"
}
```

统一入口：

```text
https://tempest07.com/bond-centre/
```

## 安全要求

资料库 API 不再在项目中心内登录，也不接受 `Authorization: Bearer APP_PASSWORD`。用户先访问 `https://tempest07.com/login/` 完成统一登录；Gateway Worker 校验 `admin` 密码后签发 `tempest07_session` cookie，并在代理到项目中心时注入短期 `X-Tempest-Auth` 签名身份。项目中心 Pages Function 只校验该签名，默认管理员用户名为 `admin`，昵称为“管理员”。

正式启用后仍建议使用 Cloudflare Access 进一步保护 Gateway 路径和 Pages 项目。直接访问 `*.pages.dev` 时，前端会跳转至 Gateway，但跳转本身不能替代访问控制。

## 隐含评级数据源

- 配置 `WIND_API_KEY` 后，中债隐含评级以 Wind 返回值为准，DM 不再作为隐含评级回退来源。
- Wind 无结果时，可使用明确标注的云端主体库历史隐含评级；不得用主体评级替代。
- 项目记录保存 `hiddenRatingSource` 和 `hiddenRatingAsOf`，用于区分 Wind、云端主体库和人工录入。
- 尚未配置 `WIND_API_KEY` 的环境继续沿用旧 DM 行为，便于先合并代码、后配置 Secret；完成 Secret 配置后即自动启用 Wind 替代。

## 二级交易录入

- 当前入口只处理公募债和 PPN：每行粘贴一笔交易要素，系统会生成网页内的待成交记录。
- 待成交卡片保留原始文本和解析字段；填入 50 至 150 之间的净价后点击“成交”，记录进入对应交易日台账。
- 带有“私募债”“非公开”或“协议转让”明确标记的交易所记录会自动分流到协议转让页。系统不会仅凭 `.SH` / `.SZ` 代码判断公募或私募。
- 旧的影子库存、OFR 挂单、一级中标同步数据继续保留在状态中，但当前界面不再展示这些入口。

## 实时行情

- “实时行情”使用独立黑色报价 Tab；主界面只展示“导入券池”按钮，不放置大文本框。导入弹窗支持逐只输入券码/券名、读取剪贴板以及 TXT/CSV 文件，也可读取“券码 简称 买入/卖出 目标收益率”并建立到价条件。
- 券池只保存在当前浏览器本地。行情由服务端使用 `INNO_APP_KEY` / `INNO_APP_SECRET` 调用 DM `/dm-quant-func-service/api/v1/bond/market-data/realtime-quote`，密钥和原始加密响应均不会下发到浏览器。
- 默认每 20 秒轮询，可切换为 15 或 30 秒；离开实时行情 Tab 后仍继续轮询。未实际展示给用户的 Bid/Ofr 变化按券、按方向累计，返回后同时标出未读次数、变动轨迹和改善/转差颜色；浏览器进入系统后台后可能受浏览器定时器节流影响。
- 中债、中证估值由独立低频快照接口调用 DM `/dm-quant-func-service/api/v1/bond/market-data/date`，显示最新可用估值日期、收益率口径和净价；含有效行权条款的债券优先使用 YTE，普通债券使用 YTM。估值不随 15–30 秒实时报价重复请求。
- 表头支持拖动换序和点击升序/降序；“列设置”可临时隐藏、重新加入或一键恢复默认列，设置保存在当前浏览器。
- 买入目标监控 OFR（收益率达到或高于目标、净价达到或低于目标），卖出目标监控 BID（收益率达到或低于目标、净价达到或高于目标）。到价后保留面板提醒；用户主动授权后，页面不在前台时还会发送浏览器系统通知。
- 点击 OFR 复制 `券码 券名 taken 价格`；点击 BID 复制 `券码 券名 given 价格`。
- 页面展示 DM 当日经纪商聚合最优 Bid/Ofr 收益率、净价、量和发布时间。DM V2.5 该接口每只债券只返回一条聚合最优行情，不返回具体中介名称，因此界面明确标记“DM 经纪商聚合”，不拆分或推断平安、中诚、国利等逐中介报价。

## Word 历史流程导入

Word 导入解析器将在取得真实文档后实现。解析时将按照文档顺序设置 `sourceRank`，数值越小代表越新；同一主体仅保留最靠前的最新授信。
