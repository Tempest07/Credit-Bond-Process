# 最终标位工作流 · 构建 4.0.0.2

## 目标与操作

区分已提交但仍需改标的项目和最终标位已确认的项目；后者可恢复继续改标。

- 提交标位：进入“已投标”，每次提交保留独立历史。
- 确认最终标位：进入“已投标结束”，移出“已投标”筛选；绑定最近一次提交，不新增提交次数。
- 继续改标：恢复“已投标”，不清空标位或历史。再次提交后可再次确认最终。
- 尚未提交的标位修改不能确认为最终；已确认最终后修改投标要素会解除最终标记。估值、定价、缴款日期等非投标要素不受影响。
- 两种已投标状态都可录入结果，截标后的结果待办也都保留。“不投标终止”是另一种状态。
- 顶部与台账筛选共用同一状态条件；“已出结果”汇总中标、未中标与缴款状态，左侧保留这些明细筛选。

## 实现与数据

- `lifecycle.js`：状态归一、最终提交 ID、标位变化检测、可逆状态操作及统一筛选计数。
- `app.js` / `index.html` / `styles.css`：操作按钮、最终标记、同步筛选和历史显示。
- `reminders.js`：两种已投标状态的结果待办。
- 项目新增 `finalBidSubmissionId`，随原有 JSON 状态保存，不需要 SQL schema 迁移。旧“已投标待结果”读取为“已投标”，不自动标记最终。
- 资源缓存标记：`20260903-bid-finalization`。本改动不调用 DM 或语义模型，不修改其配置。

## 验证

- `node --test tests/bid-finalization.test.js tests/lifecycle.test.js tests/reminders.test.js tests/assets.test.js`：91/91 通过。
- 资源版本同步后，assets、issuance-recognition、API、state-history、mailer-worker 相关回归全部通过；三个修改的业务 JS 语法检查、`git diff --check` 通过。
- 本地隔离 D1 + 浏览器：筛选联动；确认最终后移出列表；继续改标后返回；未提交修改被拦截；第二次提交及最终标记刷新后保留。
- 390px 手机视口：两个状态按钮均可操作、无横向溢出；浏览器无错误日志。未操作生产真实项目。

## 后续

代码推送 main 后由 Cloudflare Pages 发布；只读核对部署状态、生产构建及资源标记。

## 构建 4.0.0.3 · 卡片标位提示

- 目标：无需打开详情即可看到当前投标位置，缴款完成后收起。
- `lifecycle.js` 的 `projectCardBidSummary` 读取最近提交快照，保留利率精度、多档、品种名称/期限及委外管理人；修改未提交只加提示，不覆盖已提交值。
- 没有提交历史但已有结果的旧项目使用已保存投标要素，标为“投标记录”，不虚构提交次数。未投标/撤回、终止、已缴款卡片不显示，部分缴款继续保留。
- `app.js` / `styles.css` 在卡片底部渲染紧凑可换行提示，不改变数据库或交易操作。资源标记 `20260903-bid-card-summary`。
- `tests/project-card-bids.test.js` 四项业务用例及 bid-finalization/lifecycle/assets 回归通过；新增卡片渲染检查通过。1280px/1600px 桌面和 390px 手机浏览器验收：多档与双品种完整显示、无横向溢出、已缴款卡片无提示，控制台无 error。测试使用隔离的本地合成数据。
