import { writeFile } from "node:fs/promises";

const config = `# Generated by tools/prepare-local-dev.mjs for local branch previews.
# This file is ignored by git. Production Pages settings still come from Cloudflare.

name = "credit-bond-process-local"
pages_build_output_dir = "."
compatibility_date = "2026-06-09"

[[d1_databases]]
binding = "DB"
database_name = "credit-bond-process-local"
database_id = "00000000-0000-0000-0000-000000000001"
`;

await writeFile(new URL("../wrangler.toml", import.meta.url), config, "utf8");
console.log("Prepared local wrangler.toml for D1 binding DB.");
