# Task Manifest

## Goal

在新增项目的 DM 精确查询无结果时，按已确认发行人返回可点击的相关债券候选。

## Outputs

- `functions/api/dm/lookup.js`: 同发行人存续债召回、业务相关排序及诊断信息。
- `app.js`: 无本期数据状态下展示候选并支持点击后重新读取。

## Changes

- 精确债券未命中但云端主体库能确认发行人时，调用 DM 存续债接口补充候选池。
- 候选优先考虑同发行人、同主体简称、同年份、同品种及相近期次，最多返回 5 条。
- 候选只供人工点击选择，不自动覆盖本期项目字段；候选查询失败不影响原主体资料预填。
- 内部构建更新为 `4.2.0.5`，浏览器缓存键更新为 `20260904-dm-related-suggestions`。

## Verification

- `node --test tests/assets.test.js tests/dm-lookup.test.js`: 85/85 通过。
- `npm test`: 490/490 通过。
- 固定回归：`26佛水环境GN002` 可返回 `26佛水环境MTN001(绿色)`、`26佛水环境MTN002(绿色)`。
- 正式站点：输入 `26佛水环境GN002` 后显示上述 2 个候选；点击 `26佛水环境MTN001(绿色)` 可重新读取 DM 并带入新增项目。

## Next

- None.
